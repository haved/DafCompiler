// -*- compile-command: "./CompileToTest.py && ./RunTests.py -f TestFile.daf" -*-

def main:=() {
    mut a := 5;
    a = a + 30;
    if a != 35
       assert(0e3i32);
    else if a < 35
        assert(0x1);
    while a > 3 {
          a--;
    }
    a = 3 + 5 - 7;
    while (a) a();
    //It's hard coded in that a scope needn't have semicolons behind it
};