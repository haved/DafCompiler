//THis file is intended for the first version of daf, without compile time parameters

def main := (a:int, mut b:u8, uncrt c:char, dtor inst:MyClass):ctor int {
	b++;
	this = a+b;
	c = a-b;
	print(inst.deathMessage());
};

def otherFunc := () { return; };
def thirdFunc := ():let mut u8 = globalVar;
def forthFunc := ():mut= { globalChar; };
