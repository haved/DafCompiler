
// This document is intended for detailing all the syntax the parser will understand before I move on.
// This means I won't include compile time parameters, but every other syntax construct should be present.
// As this is a document for the parser only, it will not compile through any further statges.

let a:=4;
let mut b:int=20;
mut d:u8;

def a_2 := a;
def let b_2 :int= 20;
def mut d_2:u8; //Foreign

//I dunno, let's maybe use org mode instead