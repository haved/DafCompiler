let a:=5;
let uncertain mut c:(&move d:int, par:&mut MyClass);
def memcpy:(dst:&mut void, src:&void, len:int):&mut void;
def main:=(argc:int, argv:&char):int {
    {
        {
            let mut x:=5;
            def g:=x;
            let a:=(x:int,y:int):int{}(4, 5);
            print(a);
        }
    }
};