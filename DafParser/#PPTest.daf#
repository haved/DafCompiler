/*let mut a := 5;
pub def b := 5;

//This is a comment
pub def main:=() {
    printf("4+8 is #( 4 8 + #)");
    printf("true = #( true #)");
    printf("false = #( false #)");

    printf("##Test of pound symbol");
}*/
#macro Test_Macro<name;age> Hello #name! You are #age years old. ##ToBeEvaluated

/**
 This is a multiline comment,
 yo
*/

#macro VERSION 2.15
#macro Test<etwas,gut> $
#etwas#gut<>ay //The <> is used to distinguish the #gut from ay without a space
$
//#macro H<> $$

/*
def printVersion:=() {

//Code's ugly, yo!
#ifmacro VERSION
	printf("The version is: '#ifmacro Test<>#Test<#VERSION>#else<>#VERSION#endif<>'");
#endif //Does not need <> unless you wan't no spaces between the code 
}

#if<> #( #VERSION == 2.15 #) #then<> //Again. The <> is optional
      def balle := true;
#else
      def balle := false;
#endif */