#ifmacro DEBUG
import daf.assert;
#endif

#macro DEBUG

//This is a comment
pub let a:=5; //This might have been a def, unless you need to point to it

/** The main function
 * @return exit status
 */
pub def main:=(argv:int, argc:&&char) -> int {
	#ifmacro DEBUG
	assert(a==5);
	#endif
}