import daf.print;
import yo.bro;
import me . are . good;
//import what.abou.this
import me.haved;
import me;
//import yo.


/*pub def g:=10;

pub proc main() {
    let mut a :int= 5;

    printInt(a);
    printf("%d", a);

    if(a==5) {
    	a++;
    }

    a=a-(a=g); //Assignment is also an expression :/
    a+=6;

    printf("triangle of %d is %d!", a, triangle(a));
}

pub def triangle:=(n:int) -> int {
    if(n<=0)
	return 0;
    return triange(n-1)+n;
}*/